<div class="col-md-4">
  <div class="list-group">
    <div class="list-group-item item-add-bar">
      <form {{action "createBand" on="submit"}}>
        {{input type="text" class="new-band" placeholder="New Band" value=name}}
        <button type="submit" class="btn btn-primary btn-sm new-band-button" disabled={{addButtonDisabled}}>Add</button>
      </form>
    </div>
    <!--
    <div class="list-group-item item-add-bar">
      {{!input type="text" class="new-band" placeholder="Test" value=testProp}}
      <button class="btn btn-primary btn-sm new-band-button" {{!action "testFunction"}}>Add</button>
    </div>
    -->
    {{#each model as |band|}}
      {{#link-to "bands.band.songs" band class="list-group-item
band-link"}}
        {{band.name}}
        <span class="pointer glyphicon glyphicon-chevron-right"></span>
      {{/link-to}}
    {{/each}}
  </div>
</div>
<div class="col-md-8">
  {{outlet}}
</div>
